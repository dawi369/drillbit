{
  "name": "drillbit",
  "private": true,
  "workspaces": [
    "apps/api",
    "packages/contracts"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/api dev",
    "typecheck": "bun run --cwd apps/api typecheck",
    "test": "bun run --cwd apps/api test",
    "ios:generate": "xcodegen generate --spec apps/ios/project.yml",
    "contracts": "bun scripts/contracts.ts"
  },
  "devDependencies": {
    "prettier": "3"
  }
}
